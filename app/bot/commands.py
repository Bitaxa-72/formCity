START_MESSAGE = (
    "Привет, я бот formcity. Я даю информацию по данным из таблиц.\n\n"
    "Для лучшей точности указывайте тип отчета, проект, метрику и период.\n\n"
    "Важно:\n"
    "- если период не указан в платежном календаре, ответ будет за весь доступный период;\n"
    "- если период не указан в дорожной карте, ответ будет за последний актуальный месяц;\n"
    "- если период не указан в модели, ответ будет за последний актуальный срез;\n"
    "- если проект не указан, ответ будет по всем доступным проектам.\n\n"
    "Доступные отчеты:\n"
    "- платежный календарь;\n"
    "- дорожная карта;\n"
    "- модель;\n"
    "- непроектные расходы;\n"
    "- остатки в продаже;\n"
    "- отчет о продажах;\n"
    "- исполнение плана продаж;\n"
    "- отчет по агентам;\n"
    "- ДЗ и брони;\n"
    "- сводный отчет.\n\n"
    "Команда /info покажет доступные отчеты и проекты."
)

INFO_MESSAGE = (
    "Данные берутся из базы проекта.\n\n"
    "Правила по умолчанию:\n"
    "- если период не указан в платежном календаре, ответ будет за весь доступный период;\n"
    "- если период не указан в дорожной карте, ответ будет за последний актуальный месяц;\n"
    "- если период не указан в модели, ответ будет за последний актуальный срез;\n"
    "- если проект не указан, ответ будет по всем доступным проектам;\n"
    "- чтобы сбросить период в уточнении, напишите: за весь период, за все время или без периода.\n\n"
    "Доступные типы отчетов:\n"
    "- сводная;\n"
    "- модель;\n"
    "- платежный календарь;\n"
    "- дорожная карта;\n"
    "- остатки в продаже;\n"
    "- ДЗ и брони;\n"
    "- отчет о продажах;\n"
    "- отчет об исполнении плана продаж;\n"
    "- отчет по агентам;\n"
    "- непроектные расходы.\n\n"
    "По модели доступны KPI, список срезов, список raw-листов и безопасные строки raw-листов.\n\n"
    "Доступные проекты:\n"
    "- Обводный;\n"
    "- Московский;\n"
    "- Евгеньевский."
)

CLEAR_MESSAGE = "Контекст диалога очищен."
ADMIN_ONLY_MESSAGE = "Команда /admin доступна только администратору."
ADMIN_ENABLED_MESSAGE = "Admin debug включен. Буду отправлять JSON по этапам обработки."
ADMIN_DISABLED_MESSAGE = "Admin debug выключен."

COMMAND_RESPONSES = {
    "/start": START_MESSAGE,
    "/info": INFO_MESSAGE,
    "/clear": CLEAR_MESSAGE,
    "/admin": "",
}

BOT_COMMANDS = [
    {"command": "start", "description": "начать работу"},
    {"command": "info", "description": "описание доступных данных"},
    {"command": "clear", "description": "очистить контекст"},
]


def normalize_command(text: str | None) -> str | None:
    if not text:
        return None

    first_token = text.strip().split(maxsplit=1)[0].lower()
    if not first_token.startswith("/"):
        return None

    command = first_token.split("@", 1)[0]
    return command if command in COMMAND_RESPONSES else None
